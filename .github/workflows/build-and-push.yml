name: ci
on:
  push:
    branches:
      - 'master'

jobs:
  push_to_registries:
    name: Build and Push Docker image
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:

      - name: Check out the repo
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Retrieve version
        run: |
          echo "::set-output name=TAG_NAME::$(grep 'AGENT_VERSION=' Dockerfile | awk -F= '{print $2}')"
        id: version

      - name: Push to Docker Hub
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: dcagatay/azp-agent-dind:latest,dcagatay/azp-agent-dind:${{ steps.version.outputs.TAG_NAME }}

      # - name: Log in to GitHub Docker Registry
      #   uses: docker/login-action@v1
      #   with:
      #     registry: docker.pkg.github.com
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      # - name: Build container image
      #   uses: docker/build-push-action@v2
      #   with:
      #     push: true
      #     tags: docker.pkg.github.com/${{ github.repository }}/my-image:${{ github.ref }}
